import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Frontend_Mobile_Sabores_Autenticos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
